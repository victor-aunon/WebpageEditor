if errors
    section.container
        each error in errors
            - var so = "<strong>";
            - var sc = "</strong>";
            p.message.alert !{so}#{error.message}!{sc}. #{error.value}